<h1>Details</h1>

  Name: <%= @product.name %> <br>
  Description: <%= @product.description %> <br>
  Price: <%= @product.price_in_dollars %> <br>
  <p>
    <%= link_to "Edit", "/products/#{@product.id}/edit" %>
    | <%= link_to "Delete", "/products/#{@product.id}", method: :delete, data: {Confirm: "Are you sure?"} %>
  </p>

<h2>Reviews</h2>

<% @product.reviews.each do |review| %>
Comment: <%= review.comment %> <br>
Created at: <%= review.created_at.localtime.strftime("%H:%M @ %m/%d/%y") %> <br>
<%= link_to "Edit", "/products/#{@product.id}/reviews/#{review.id}/edit" %>
| <%= link_to "Delete", "/products/#{@product.id}/reviews/#{review.id}", method: :delete, data: {Confirm: "Are you sure?"} %> <br>
<br>
<% end %>


<h4>New Review</h4>

  <%= render 'reviews/form' %>
